version: '3.7'

services:
  web:
    build: .
    command: python /usr/src/app/manage.py runserver 0.0.0.0:8000 --settings=gss.settings.docker
    ports:
      - 8000:8000
    depends_on:
      - db
    volumes:
      - /static:/usr/src/app/static
  db:
    image: postgres:11.1-alpine